import React from 'react';

interface ChatProps {
  sessionId?: string;
}

function Chat({ sessionId }: ChatProps) {
  return (
    <div className="chat-container">
      {/* Chat implementation */}
    </div>
  );
}

export default Chat;
