interface ChatProps {
  sessionId?: string;
}

function Chat({ sessionId: _sessionId }: ChatProps) {
  return (
    <div className="chat-container">
      {/* Chat implementation */}
    </div>
  );
}

export default Chat;
